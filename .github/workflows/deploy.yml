name: PulseNode Deployment & Verification

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Compile & Verify Agent
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Install Build Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake ninja-build pkg-config g++ libuv1-dev liblz4-dev libssl-dev zlib1g-dev libmnl-dev uuid-dev libzstd-dev libelf-dev libpcre2-dev libcurl4-openssl-dev

    - name: Configure PulseNode
      run: |
        mkdir build
        cd build
        cmake .. -G Ninja -DCMAKE_INSTALL_PREFIX=/usr/local/pulsenode

    - name: Build Core Engine
      run: |
        cd build
        ninja

    - name: Verify Startup Sequence
      run: |
        ./build/src/daemon/netdata -W buildinfo | grep "PulseNode"
        echo "✅ PulseNode Core Branding Verified"
